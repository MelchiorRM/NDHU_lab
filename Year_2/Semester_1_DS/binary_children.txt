Complete this function for a binary tree.In the array representation of a binary tree, assuming the root index is 0.
void findChildren(int index, int size, int& left, int& right) {
    // TODO: Calculate indices
    // Formula: Left = 2*i + 1, Right = 2*i + 2
    // Check bounds against 'size'
    
    left = -1;
    right = -1;
}
input
10
2
3
4 
output
7 8
9 -1