Given a general tree (each node can have an arbitrary number of children), calculate the height of the entire tree.

Definition: The height of a leaf node is 0. The height of the tree is equal to the height of the root node.

input:
5
1 2
1 3
2 4
2 5
output:
2
To complete
struct Node {
    int id;
    vector<Node*> children;
};

int getHeight(Node* root) {
    if (root == nullptr) return -1;
    if (root->children.empty()) return 0; // Leaf node height is 0

    int maxChildHeight = -1; 
    
    // TODO: Iterate through children, find max height
    // Height = 1 + max(children's height)
    
    return 0; // Replace this
}
