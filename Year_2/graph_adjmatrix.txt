Given the number of vertices N and a list of edges for an undirected graph, construct and output the corresponding N×N Adjacency Matrix.

If vertex u and v are connected, matrix entries [u][v]and[v][u]should be 1, otherwise 0. Vertex indices start from 0.
input
3 2
0 1
1 2
The first line contains two integers N,M, representing the number of vertices and edges.
The next M lines each contain two integers u,v, representing an edge.
Output
0 1 0
1 0 1
0 1 0
N lines, each containing N integers (0 or 1), separated by spaces.
Complete this function
vector<vector<int>> buildMatrix(int n, vector<pair<int, int>>& edges) {
    vector<vector<int>> matrix(n, vector<int>(n, 0));

    // TODO: Iterate through edges and update matrix
    // Remember: It's an undirected graph
    
    return matrix;
}
